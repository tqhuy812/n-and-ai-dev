interface:
- admin-state: enable
  name: ethernet-1/1
  subinterface:
    - admin-state: enable
      description: To client1
      index: 0
      ipv4:
        admin-state: enable
        address:
          ip-prefix: 10.0.1.1/30

- admin-state: enable
  name: ethernet-1/2
  subinterface:
    - admin-state: enable
      description: To srlinux2
      index: 0
      ipv4:
        admin-state: enable
        address:
          ip-prefix: 10.0.12.0/31

- admin-state: enable
  name: ethernet-1/3
  subinterface:
    - admin-state: enable
      description: To srlinux3
      index: 0
      ipv4:
        admin-state: enable
        address:
          ip-prefix: 10.0.13.0/31
